- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."user_roles"("userId" integer NOT NULL, "role" text
      NOT NULL, PRIMARY KEY ("userId","role") , FOREIGN KEY ("role") REFERENCES "public"."enum_user_role"("role")
      ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: user_roles
    schema: public
  type: add_existing_table_or_view
